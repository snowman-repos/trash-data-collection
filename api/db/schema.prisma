datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = "native"
}

model Record {
  id        Int     @id @default(autoincrement())
  date      String
  group     String
  location  String
  data      String
}
